import { DefaultConfig } from "./utils";

export const defaultConfig: DefaultConfig = {
    channel: "",
    loadOnStart: {
        auth: true,
        // cart: true,
        checkout: true,
    },
};
